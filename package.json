{
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "scripts": {
    "start": "json-server --host localhost --watch db.json",
    "reset": "copy /Y db.json.backup db.json",
    "start-with-reset": "npm run reset && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
